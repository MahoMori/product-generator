import styled from "styled-components";
import { color, PanelStyle } from "../../assets/style/styleVariables";

export const PreviewPanelDiv = styled(PanelStyle)`
  background-color: ${color.pink};

  box-shadow: -5px 1px 10px -2px #858585 inset;
`;
